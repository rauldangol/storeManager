@model IEnumerable<Category>

@if (Model.Count() > 0)
{
    <h2>Search Results</h2>
    <table class="table table-bordered tabletable-striped" style="width:100%">
        <thead>
            <tr>
                <th>Category Name</th>
                <th>Category Quantity</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td style="width:40%">
                        @obj.categoryName
                    </td>
                    <td style="width:10%">
                        @obj.categoryQuantity
                    </td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Category" asp-action="Edit" asp-route-productid="@obj.Id"
                       class="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i> Edit</a>
                            <a asp-controller="Category" asp-action="Delete" asp-route-prdid="@obj.Id"
                       class="btn btn-danger mx-2"><i class="bi bi-trash"></i> Delete</a>


                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No results found.</p>
}